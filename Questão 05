valor_compra = float(input('Digite um valor:'))
cont = 0
for prestacao in range(1,25):
    cont += 1
    if True:
        pagamento_sem_juros = valor_compra/prestacao
        print(f'{cont}x de R${pagamento_sem_juros:.2f}')
